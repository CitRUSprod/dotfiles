---
description: Ревьюит открытый PR через @code-reviewer и публикует комментарий
---

Текущая ветка: !`git branch --show-current`

Шаги:

1. **Проверь окружение:**
    - Проверь, доступен ли `gh` CLI (`gh --version`). Если недоступен — сообщи об ошибке и прекрати выполнение
    - Проверь авторизацию `gh` (`gh auth status`). Если не авторизован — сообщи об ошибке и предложи выполнить `gh auth login`, затем прекрати выполнение

2. **Определи PR:**
    - Если передан аргумент и он не пуст (`$ARGUMENTS` определён и не является пустой строкой):
        - Проверь, что это либо число (только цифры), либо URL вида `https://github\.com/[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+/pull/\d+`
        - Если это число — используй его как номер PR
        - Если это URL — извлеки номер PR из URL
        - Если аргумент не соответствует ни одному формату — сообщи пользователю об ошибке: "Некорректный аргумент. Укажите номер PR (число) или ссылку на PR вида https://github.com/owner/repo/pull/123" и прекрати выполнение
    - Иначе получи PR для текущей ветки: `gh pr view --json number,url,headRefName,baseRefName,title`
    - Если команда завершилась ошибкой или PR не найден — сообщи пользователю и прекрати выполнение

3. **Получи изменения:**
    - Выполни `gh pr diff <number>` и сохрани результат в переменную
    - Если команда завершилась ошибкой — сообщи пользователю текст ошибки (выведи stderr) и прекрати выполнение
    - Если diff пуст — сообщи пользователю что в PR нет изменений и прекрати выполнение
    - Очисти diff от бинарных файлов: удали строки, содержащие `Binary files` и соответствующие заголовки `diff --git` для бинарных файлов
    - Если количество строк diff превышает 2000 или общая длина diff превышает 50000 символов — предупреди пользователя: "Diff содержит N строк (общая длина M символов). Это может превысить контекстное окно LLM." и спроси, хочет ли он продолжить

4. **Запусти ревью:**
    - Передай результат агенту `@code-reviewer` для анализа
    - Дождись результата

5. **Проверь результат ревью:**
    - Убедись, что результат содержит ожидаемую структуру (наличие эмодзи критичности: 🔴, 🟠, 🟡, ⚪ или маркер ✅)
    - Если результат не содержит ни одного из этих маркеров и не содержит ✅ — скорее всего, @code-reviewer вернул ошибку
    - При невалидном результате — сообщи пользователю текст ошибки и не публикуй комментарий

6. **Опубликуй результат:**
    - Сохрани результат ревью во временный файл и передай его через --body-file:
        ```
        TMPFILE=$(mktemp)
        printf '%s\n' "$REVIEW_RESULT" > "$TMPFILE"
        if gh pr comment <number> --body-file "$TMPFILE"; then
            rm "$TMPFILE"
        else
            echo "Результат ревью сохранён в $TMPFILE"
        fi
        ```
    - Если команда `gh pr comment` завершилась ошибкой — сообщи пользователю текст ошибки и путь к файлу с результатом ревью (`$TMPFILE`), чтобы он мог опубликовать его вручную
    - Сообщи пользователю ссылку на PR и что ревью опубликовано
